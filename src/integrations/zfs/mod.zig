/// ZFS integration module
///
/// This module provides integration with ZFS for container checkpoint/restore operations.
pub const types = @import("types.zig");
pub const client = @import("client.zig");
