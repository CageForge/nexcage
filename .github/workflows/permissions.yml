# DISABLED: This workflow is deprecated
# Use individual workflow permissions instead
name: "Permissions (Disabled)"

on:
  workflow_dispatch:
    # Disabled - use individual workflow permissions

jobs:
  disabled:
    name: "Workflow Disabled"
    runs-on: [self-hosted, proxmox]
    if: false  # Always skip
    steps:
      - name: "This workflow is disabled"
        run: echo "This workflow has been disabled in favor of individual workflow permissions" 