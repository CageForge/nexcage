# DISABLED: This workflow is deprecated
# Use CI (CNCF Compliant) or other specific workflows instead
name: CI (Disabled)

on:
  workflow_dispatch:
    # Disabled - use CI (CNCF Compliant) instead

permissions:
  contents: read
  packages: read
  security-events: write

jobs:
  disabled:
    name: Workflow Disabled
    runs-on: ubuntu-latest
    if: false  # Always skip
    steps:
      - name: This workflow is disabled
        run: echo "This workflow has been disabled. Use 'CI (CNCF Compliant)' or other specific workflows instead."